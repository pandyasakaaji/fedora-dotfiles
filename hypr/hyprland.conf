
# #######################################################################################
# AUTOGENERATED HYPRLAND CONFIG.
# EDIT THIS CONFIG ACCORDING TO THE WIKI INSTRUCTIONS.
# #######################################################################################

#autogenerated = 1 # remove this line to remove the warning

# This is an example Hyprland config file.
# Refer to the wiki for more information.
# https://wiki.hypr.land/Configuring/

# Please note not all available settings / options are set here.
# For a full list, see the wiki

# You can split this configuration into multiple files
# Create your files separately and then link them to this file like this:
# source = ~/.config/hypr/myColors.conf


################
### MONITORS ###
################

# See https://wiki.hypr.land/Configuring/Monitors/
monitor=,preferred,auto,1


###################
### MY PROGRAMS ###
###################

# See https://wiki.hypr.land/Configuring/Keywords/

# Set programs that you use
$terminal = kitty
#$fileManager = nautilus
# $menu = hyprlauncher


#################
### AUTOSTART ###
#################
exec-once = swaybg -i /home/kisaragi/Pictures/Wallpapers\ Cycle/wallpaper.png -m fill
exec-once = sleep 1.3 && hyprlock
exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once = systemctl --user import-environment WAYLAND_DISPLAY XDG_CURRENT_DESKTOP

# --- CORE SERVICES ---
exec-once = hypridle
exec-once = /usr/libexec/hyprpolkitagent

# --- UI & UTILITIES ---
exec-once = waybar
exec-once = wl-paste --watch cliphist store
exec-once = udiskie --tray &
# exec-once = hyprpm reload
exec-once = sudo ydotoold --socket-path=$XDG_RUNTIME_DIR/.ydotool_socket

# --- APPLETS ---
exec-once = sleep 2 && nm-applet --indicator
exec-once = sleep 2 && blueman-applet

# --- SCRIPTS ---
exec-once = ~/.config/hypr/scripts/low_bat_notifier.sh

# --- AUTO WORKSPACE ---
exec-once = hyprctl dispatch exec "[workspace 1 silent; float; size 720 450; center] kitty --title fastfetch-popup sh -c 'fastfetch -c examples/27; exec fish'"
# exec-once = [workspace 1 silent] $terminal sh -c "fastfetch -c examples/27; exec fish"
# exec-once = [workspace 1 silent] $terminal 
# exec-once = [workspace 2 silent] $terminal 
# exec-once = [workspace 3 silent] firefox

#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hypr.land/Configuring/Environment-variables/

# Default
env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24

# Default Apps
env = EDITOR,nvim
env = VISUAL,nvim

# XDG & Session
env = XDG_CURRENT_DESKTOP,Hyprland
env = XDG_SESSION_TYPE,wayland
env = XDG_SESSION_DESKTOP,Hyprland

# Wayland Backends
env = GDK_BACKEND,wayland,x11,*
env = QT_QPA_PLATFORM,wayland;xcb
env = MOZ_ENABLE_WAYLAND,1
env = ELECTRON_OZONE_PLATFORM_HINT,auto

# GTK
env = GTK_APPLICATION_PREFER_DARK_THEME,1
env = GTK_A11Y,none

# Qt
env = QT_QPA_PLATFORMTHEME,qt6ct

# Cursor
env = XCURSOR_THEME,Nordzy-cursors
env = XCURSOR_SIZE,24
env = HYPRCURSOR_THEME,Nordzy-cursors
env = HYPRCURSOR_SIZE,24

# Scroll binding
# env = YDOTOOL_SOCKET,$XDG_RUNTIME_DIR/.ydotool_socket

###################
### PERMISSIONS ###
###################

# See https://wiki.hypr.land/Configuring/Permissions/
# Please note permission changes here require a Hyprland restart and are not applied on-the-fly
# for security reasons

# ecosystem {
#   enforce_permissions = 1
# }

# permission = /usr/(bin|local/bin)/grim, screencopy, allow
# permission = /usr/(lib|libexec|lib64)/xdg-desktop-portal-hyprland, screencopy, allow
# permission = /usr/(bin|local/bin)/hyprpm, plugin, allow


#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hypr.land/Configuring/Variables/

# https://wiki.hypr.land/Configuring/Variables/#general
general {
    # gaps_in = 5
    # gaps_out = 20
    gaps_in = 7
    gaps_out = 24

    border_size = 2

    # https://wiki.hypr.land/Configuring/Variables/#variable-types for info about colors
    # col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    # col.inactive_border = rgba(595959aa)
    # col.active_border = rgba(5E81ACFF) rgba(729FCFFF) 90deg
	# # col.active_border = rgba(dadadaaa)
	# # col.inactive_border = rgba(7aa2f755)
	# col.inactive_border = rgba(00000000)

	col.active_border = rgba(5E81ACFF) rgba(88C0D0FF) 45deg
	col.inactive_border = rgba(4C566Aaa)

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = false

    # Please see https://wiki.hypr.land/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    layout = dwindle
}

# https://wiki.hypr.land/Configuring/Variables/#decoration
decoration {
    rounding = 15
    rounding_power = 2

    # Change transparency of focused and unfocused windows
    active_opacity = 1
    inactive_opacity = 0.67

    shadow {
        enabled = false
        # range = 4
        # render_power = 3
        # color = rgba(1a1a1aee)
    }

    # https://wiki.hypr.land/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 8
        passes = 2
		new_optimizations = true
        ignore_opacity = true
		special = false
		popups = false
    }
}

# # https://wiki.hypr.land/Configuring/Variables/#animations
# animations {
#     enabled = yes, please :)
#
#     # Default curves, see https://wiki.hypr.land/Configuring/Animations/#curves
#     #        NAME,           X0,   Y0,   X1,   Y1
#     bezier = easeOutQuint,   0.23, 1,    0.32, 1
#     bezier = easeInOutCubic, 0.65, 0.05, 0.36, 1
#     bezier = linear,         0,    0,    1,    1
#     bezier = almostLinear,   0.5,  0.5,  0.75, 1
#     bezier = quick,          0.15, 0,    0.1,  1
# 	# New
# 	bezier = fluent_decel, 0, 0.2, 0.4, 1
#     bezier = easeOutExpo, 0.16, 1, 0.3, 1
#
#     # Default animations, see https://wiki.hypr.land/Configuring/Animations/
#     #           NAME,          ONOFF, SPEED, CURVE,        [STYLE]
#     animation = global,        1,     10,    default
#     animation = border,        1,     5.39,  easeOutQuint
#     animation = windows,       1,     4.79,  easeOutQuint
#     animation = windowsIn,     1,     4.1,   easeOutQuint, popin 87%
#     animation = windowsOut,    1,     1.49,  linear,       popin 87%
#     animation = fadeIn,        1,     1.73,  almostLinear
#     animation = fadeOut,       1,     1.46,  almostLinear
#     animation = fade,          1,     3.03,  quick
#     animation = layers,        1,     3.81,  easeOutQuint
#     animation = layersIn,      1,     4,     easeOutQuint, fade
#     animation = layersOut,     1,     1.5,   linear,       fade
#     animation = fadeLayersIn,  1,     1.79,  almostLinear
#     animation = fadeLayersOut, 1,     1.39,  almostLinear
#     animation = zoomFactor,    1,     7,     quick
#
#     # Workspaces
# 	# Default
#     animation = workspaces,    1,     1.94,  almostLinear, fade
# 	# Custom
# 	# animation = workspaces,	   1,     7,     fluent_decel, slidefade 20%
#
# 	# Default
#     animation = workspacesIn,  1,     1.21,  almostLinear, fade
#     animation = workspacesOut, 1,     1.94,  almostLinear, fade
# 	# Custom
# 	# animation = workspacesIn,  1,	  6,     easeOutExpo, slidefade 20%
# 	# animation = workspacesOut, 1,     8,     easeOutExpo, slidefade 20%
#
#
# }

animations {
    enabled = yes

    bezier = mac,0.25,0.1,0.25,1

    animation = windows,1,3,mac,popin 87%
    animation = fade,1,3,mac
    animation = workspaces,1,2,mac,slidefade
    animation = border,1,3,default
    animation = specialWorkspace,0
}

# Ref https://wiki.hypr.land/Configuring/Workspace-Rules/
# "Smart gaps" / "No gaps when only"
# uncomment all if you wish to use that.
# workspace = w[tv1], gapsout:0, gapsin:0
# workspace = f[1], gapsout:0, gapsin:0
# windowrule {
#     name = no-gaps-wtv1
#     match:float = false
#     match:workspace = w[tv1]
#
#     border_size = 0
#     rounding = 0
# }
#
# windowrule {
#     name = no-gaps-f1
#     match:float = false
#     match:workspace = f[1]
#
#     border_size = 0
#     rounding = 0
# }

# See https://wiki.hypr.land/Configuring/Dwindle-Layout/ for more
dwindle {
    preserve_split = true # You probably want this
}

# See https://wiki.hypr.land/Configuring/Master-Layout/ for more
master {
    new_status = master
}

# https://wiki.hypr.land/Configuring/Variables/#misc
misc {
    force_default_wallpaper = 0 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = true # If true disables the random hyprland logo / anime girl background. :(
	disable_splash_rendering = true
}

debug {
    vfr = true
}

#############
### INPUT ###
#############

# https://wiki.hypr.land/Configuring/Variables/#input
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 1

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = true
    }
}

# See https://wiki.hypr.land/Configuring/Gestures
gesture = 3, horizontal, workspace

# Example per-device config
# See https://wiki.hypr.land/Configuring/Keywords/#per-device-input-configs for more
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}


###################
### KEYBINDINGS ###
###################

# See https://wiki.hypr.land/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier

# Example binds, see https://wiki.hypr.land/Configuring/Binds/ for more
# bind = $mainMod, M, exec, command -v hyprshutdown >/dev/null 2>&1 && hyprshutdown || hyprctl dispatch exit
bind = $mainMod, RETURN, exec, $terminal
bind = $mainMod SHIFT, RETURN, exec, $terminal --class floatingterm
bind = $mainMod, B, exec, firefox
bind = $mainMod, E, exec, $terminal --title YAZI_FLOAT yazi
bind = $mainMod, Y, togglefloating,
bind = $mainMod, F, fullscreen, 1
bind = $mainMod SHIFT, F, fullscreen,
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, space, exec, pkill wofi || wofi --show drun --width 500 --allow-images --insensitive
bind = $mainMod, V, exec, pkill wofi || cliphist list | wofi --dmenu --width 950 --height 500 | cliphist decode | wl-copy
bind = $mainMod ALT, E, exec, thunar
bind = $mainMod CTRL, L, exec, hyprlock
bind = $mainMod SHIFT, Q, killactive,
bind = $mainMod SHIFT, Y, layoutmsg, togglesplit # dwindle
bind = $mainMod SHIFT, S, exec, ~/.local/bin/snip
bind = $mainMod SHIFT, E, exec, ~/.local/bin/powermenu
bindl = $mainMod, semicolon, exec, playerctl play-pause
bindl = $mainMod, comma, exec, playerctl previous
bindl = $mainMod, period, exec, playerctl next
bind = $mainMod, C, centerwindow,
bind = $mainMod SHIFT, P, exec, hyprctl dispatch pin && notify-send -r 9995 "📌 Window" "Pin toggled"

# Move focus with mainMod + arrow keys
bind = $mainMod, h, movefocus, l
bind = $mainMod, l, movefocus, r
bind = $mainMod, k, movefocus, u
bind = $mainMod, j, movefocus, d

# Move window per workspace
bind = $mainMod SHIFT, h, swapwindow, l
bind = $mainMod SHIFT, l, swapwindow, r
bind = $mainMod SHIFT, k, swapwindow, u
bind = $mainMod SHIFT, j, swapwindow, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10
bind = $mainMod, TAB, workspace, previous
bind = $mainMod SHIFT, TAB, movetoworkspace, previous

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
bind = $mainMod, N, togglespecialworkspace, magic
bind = $mainMod SHIFT, N, movetoworkspace, special:magic

# Scroll through existing workspaces with ma nMod + scroll
# bind = $mainMod, mouse_down, workspace, e+1
# bind = $mainMod, mouse_up, workspace, e-1

# Resize keybind
bind = $mainMod, R, submap, resize
bindm = $mainMod SHIFT, R, resizewindow
submap = resize

binde = , h, resizeactive, -35 0
binde = , l, resizeactive, 35 0
binde = , k, resizeactive, 0 -35
binde = , j, resizeactive, 0 35

bind = , escape, submap, reset
bind = , RETURN, submap, reset

submap = reset

# =======================================================================================
# 🖱️ VIRTUAL SCROLL VIA YDOTOOL (Sudo Mode)
# =======================================================================================
binde = $mainMod, U, exec, sudo env YDOTOOL_SOCKET=$XDG_RUNTIME_DIR/.ydotool_socket ydotool mousemove --wheel -- 0 -2
binde = $mainMod, I, exec, sudo env YDOTOOL_SOCKET=$XDG_RUNTIME_DIR/.ydotool_socket ydotool mousemove --wheel 0 2

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Laptop multimedia keys for volume and LCD brightness
# bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
# bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
# bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
# bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
# bindel = ,XF86MonBrightnessUp, exec, brightnessctl -e4 -n2 set 5%+
# bindel = ,XF86MonBrightnessDown, exec, brightnessctl -e4 -n2 set 5%-

# ======================
# 🔊 AUDIO CONTROLS (Fix Hyprland Parser Key)
# ======================
bindlu = $mainMod, M, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle && sh -c 'if wpctl get-volume @DEFAULT_AUDIO_SINK@ | grep -q "MUTED"; then notify-send -r 9993 "󰖁 Volume" "Muted"; else vol=$(wpctl get-volume @DEFAULT_AUDIO_SINK@ | awk "{print int(\$2*100)}"); notify-send -r 9993 "  Volume" "Unmuted ($vol\%)"; fi'
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 2%+ && sh -c 'vol=$(wpctl get-volume @DEFAULT_AUDIO_SINK@ | awk "{print int(\$2*100)}"); notify-send -r 9991 "  Volume" "$vol\%"'
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 2%- && sh -c 'vol=$(wpctl get-volume @DEFAULT_AUDIO_SINK@ | awk "{print int(\$2*100)}"); notify-send -r 9991 "  Volume" "$vol\%"'
bindl = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle && sh -c 'if wpctl get-volume @DEFAULT_AUDIO_SINK@ | grep -q "MUTED"; then notify-send -r 9993 "󰖁 Volume" "Muted"; else vol=$(wpctl get-volume @DEFAULT_AUDIO_SINK@ | awk "{print int(\$2*100)}"); notify-send -r 9993 "  Volume" "Unmuted ($vol\%)"; fi'
bindl = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle && sh -c 'if wpctl get-volume @DEFAULT_AUDIO_SOURCE@ | grep -q "MUTED"; then notify-send -r 9994 "  Mic" "Muted"; else notify-send -r 9994 " Mic" "Active"; fi'

# ======================
# 🔆 BRIGHTNESS CONTROLS
# ======================
bindel = ,XF86MonBrightnessUp, exec, brightnessctl set 2%+ && p=$(brightnessctl -m | cut -d',' -f4 | tr -d '%') && notify-send -r 9992 "󰃟 Brightness" "$p%"
bindel = ,XF86MonBrightnessDown, exec, brightnessctl set 2%- && p=$(brightnessctl -m | cut -d',' -f4 | tr -d '%') && notify-send -r 9992 "󰃟 Brightness" "$p%"

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hypr.land/Configuring/Window-Rules/ for more
# See https://wiki.hypr.land/Configuring/Workspace-Rules/ for workspace rules

# Example windowrules that are useful

windowrule {
    # Ignore maximize requests from all apps. You'll probably like this.
    name = suppress-maximize-events
    match:class = .*

    suppress_event = maximize
}

windowrule {
    # Fix some dragging issues with XWayland
    name = fix-xwayland-drags
    match:class = ^$
    match:title = ^$
    match:xwayland = true
    match:float = true
    match:fullscreen = false
    match:pin = false

    no_focus = true
}

# Hyprland-run windowrule
windowrule {
    name = move-hyprland-run

    match:class = hyprland-run

    move = 20 monitor_h-120
    float = yes
}

windowrule {
    name = dialogs-floating
    match:class = ^(xdg-desktop-portal-gtk)$

    float = true
    center = true
    size = "60% 70%"

    opacity = 0.85
}

windowrule {
    name = yazi-float

    match:initial_title = ^YAZI_FLOAT$

    float = on
    center = on
    size = (monitor_w*0.5) (monitor_h*0.6)
}

windowrule {
    name = kitty-floating

    match:class = ^(floatingterm)$

    float = true
    center = true
    size = 720 450
    opacity = 0.77
}

windowrule {
    name = blueman-floating
    match:class = ^(blueman-manager)$

    float = true
    center = true

    opacity = 0.85
}

windowrule {
    name = thunar-floating
    match:class = ^(thunar)$

    float = true
    center = true

    size = 1100 700
    opacity = 0.85
}

windowrule {
    name = kitty-opacity

    match:class = ^(kitty)$

    opacity = 0.85 0.77
}

windowrule {
    name = magic-kitty

    match:class = ^(kitty)$
    match:workspace = name:special:magic

    float = true
    center = true
    size = 1200 750
}

windowrule {
    name = firefox-magic

    match:class = ^(org.mozilla.firefox)$
    match:workspace = special:magic

    float = true
    center = true
    size = 1650 800
}

layerrule {
	name = wofi-blur
    match:namespace = wofi
    blur = on
    ignore_alpha = 0.1
}

layerrule {
	name = waybar-blur
    match:namespace = waybar
    blur = on
    ignore_alpha = 0.2
}

layerrule {
	name = dunst-blur
    match:class = dunst
    blur = true
    ignore_alpha = 0.2
}

layerrule {
    name = slurp-fix

    match:namespace = selection

    blur = false
}

###############
### PLUGINS ###
###############

plugin {
    hyprbars {
        bar_height         = 34
        bar_color          = rgba(5E81AC00)
        col.text           = rgba(F5F5F7ff)
        bar_text_size      = 13
        bar_text_weight    = bold
        bar_padding        = 24
        bar_button_padding = 7
        bar_title_enabled  = false
        bar_part_of_window = false
        bar_precedence_over_border = false
        bar_buttons_alignment = left

        hyprbars-button = rgb(BF616A), 12, , hyprctl dispatch killactive
        hyprbars-button = rgb(EBCB8B), 12, , hyprctl dispatch togglefloating
        hyprbars-button = rgb(A3BE8C), 12, , hyprctl dispatch fullscreen 1
    }
}

# windowrule {
#     name            = no-bars-tiled
#     match:float     = 0
#     hyprbars:no_bar = true
# }
#
# windowrule {
#     name            = no-bars-float-non-kitty
#     match:float     = 1
#     match:class     = ^(?!kitty).*$
#     hyprbars:no_bar = true
# }
